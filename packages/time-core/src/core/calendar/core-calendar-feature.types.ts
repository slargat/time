/**
 * Types for `coreCalendarFeature`. The navigation APIs it installs
 * (`goToNextPeriod`, `changeViewMode`, …) live on `Calendar_Core` in
 * `types/Calendar.ts` since they are always present. This file is the home for
 * any core-calendar-only option/state types as they appear.
 *
 * ponytail: empty until the core feature needs its own option/state types.
 */
export {}
